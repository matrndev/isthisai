<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Entry Details - IsThisAI</title>
    <link rel="stylesheet" href="/node_modules/bootstrap/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <h1>Entry Details: <%= document.topic %></h1>

        <div class="card mb-4">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="mb-0">Summary Information</h5>
                <form action="/delete/<%= document._id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this record?');">
                    <button type="submit" class="btn btn-danger">Delete Entry</button>
                </form>
            </div>
            <div class="card-body">
                <p><strong>Model:</strong> <%= document.model %></p>
                <p><strong>Platform:</strong> <%= document.platform %></p>
                <p><strong>Date Created:</strong> <%= new Date(document.dateCreated).toLocaleString() %></p>
                <% if (document.wikiURL) { %>
                    <p><strong>Wikipedia URL:</strong> <a href="<%= document.wikiURL %>" target="_blank"><%= document.wikiURL %></a></p>
                <% } %>
            </div>
        </div>

        <div class="row">
            <div class="col-md-6">
                <div class="card mb-4">
                    <div class="card-header">
                        <h5>Wikipedia Summary</h5>
                    </div>
                    <div class="card-body">
                        <p><%= document.wikiSummary %></p>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card mb-4">
                    <div class="card-header">
                        <h5>AI Summary</h5>
                    </div>
                    <div class="card-body">
                        <p><%= document.AISummary %></p>
                    </div>
                </div>
            </div>
        </div>

        <div class="mb-4">
            <a href="/database" class="btn btn-secondary">Back to Database</a>
        </div>
    </div>

    <script src="/node_modules/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>