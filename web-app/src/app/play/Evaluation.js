export default function Evaluation({ gameStatus }) {
    return (
        <div className={`alert alert-${gameStatus ? "success" : "danger"} mt-4 text-center fade-in`}>
            <h4>{`You were ${gameStatus ? "correct!" : "incorrect :("}`}</h4>
            <p>The selected text was {gameStatus ? "indeed" : "not"} generated by AI.</p>
            <button className={`btn btn-outline-${gameStatus ? "success" : "danger"} fade-in`} style={{ width: "100%" }} onClick={() => { location.reload() }}>Continue</button>
        </div>
    )
}